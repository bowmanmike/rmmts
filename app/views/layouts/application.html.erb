<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "rmmts" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <div data-sticky-container>
      <div class="top-bar" data-sticky data-options="marginTop:0;">
        <div class="top-bar-left">
          <ul class="dropdown menu" data-dropdown-menu>
            <li class="menu-text">
              <%= link_to "rmmts", root_path %>
            </li>
            <li><%= link_to "Houses", houses_path %></li>
            <li><%= link_to "Mates", mates_path %></li>
            <li><%= link_to "Mailbox", conversations_path %></li>
          </ul>
        </div>

        <div class="top-bar-right">
          <ul class="menu">
            <% if current_user %>
              <li>Logged in as <%= current_user.username %></li>
              <li><%= link_to "Log out", logout_path(@mate), method: :post %></li>
            <% else %>
              <li><%= link_to "Login", new_mate_path %></li>
              <li><%= link_to "Sign up", new_session_path %></li>
            <% end %>
            <!-- <li><input type="search" placeholder="Search"></li>
            <li><button type="button" class="button">Search</button></li> -->
          </ul>
        </div>
      </div>
    </div>

    <%= yield %>

  </body>
</html>
