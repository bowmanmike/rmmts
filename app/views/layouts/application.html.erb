<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "chortal" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>

    <link href='https://fonts.googleapis.com/css?family=Josefin+Sans' rel='stylesheet' type='text/css'>
  </head>

  <body>
    <% if current_user %>
    <div class="top-bar">
      <div class="top-bar-left">
        <%= link_to "chortal", root_path, class: "site-logo" %>
        <% if current_user %>
          <%= link_to "house", house_path(current_user.house) if current_user.house.present? %>
          <%= link_to "houses", houses_path if current_user.house.blank? %>
          <%= link_to "messages", conversations_path %>
        <% end %>
      </div>
      <div class="top-bar-right">
        <% if current_user %>
          Logged in as <%= current_user.username %> |
          <%= link_to "View Profile", mate_path(current_user) %> |
          <%= link_to "Edit Profile", edit_mate_path(current_user), id: 'edit-mate-link', remote: true %> |
          <%= link_to "Log out", logout_path, method: :post %>
        <% else %>
          <%= link_to "Login", new_session_path, id: "login-link", remote: true %>
          <%= link_to "Sign up", new_mate_path, id: "signup-link", remote: true %>
        <% end %>
      </div>
    </div>
    <% end %>

    <div id="overlay"></div>

    <div class="popup">
      <div class="popup-layout">
        <span class="close-popup-link">close [x]</span>
      </div>
      <div class="popup-form"></div>
    </div>

    <div class="content">
      <%= yield %>
    </div>

  </body>
</html>
