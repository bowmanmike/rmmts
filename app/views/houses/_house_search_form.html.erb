<%= form_tag houses_path, method: :get, remote: true, id: "house-search-form" do %>

  <div class="field">
    <%= text_field_tag :search, params[:search], placeholder: "house name", class: "query" %>
  </div>

  <div class="action">
    <%= submit_tag "Search" %>
  </div>

<% end %>
